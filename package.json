{
  "name": "spotify-obs-overlay",
  "version": "1.0.0",
  "description": "Local web overlay for OBS showing Spotify now playing with OAuth and glass UI",
  "private": true,
  "type": "module",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "start:clean": "node scripts/free-port.mjs && node server/index.js",
    "dev": "node --watch server/index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.0"
  }
}
